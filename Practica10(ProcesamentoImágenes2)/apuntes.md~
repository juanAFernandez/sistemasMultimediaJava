Práctica 10

En la anterior probamos dos operaciones y en esta AfineTransformOP, LookUPOP (FUNCIONES) , BandCombineOp y ColorConvertOp.

Primera fase: aplicar las operaciones de forma estática

Segunda fase: de forma automática ajustar las operaciones.

Aunque ya tengamos incluida la librería añadir la nueva versión porque hay cambiada una cosa en el LookupTableProducer.

El código anterior a varaiación del contraste y el siguiente son equivalentes.


El ángulo que le pasemos tenemos que darlo en radianes.

La w determina la frecuencia.
Al no ser una función monótona creciente el valor máximo es el 255.

P9: En caso de tener canal alfa hay que usar el segundo constructor donde pasamos los canales de alfa.


Apunte:

para sacar el tipo de color

.getColorMode().hasAlpha() nos dice si la imagen tiene alfa.

P9

if(imgSource.getColoModel().hasAlpha(){
	float scales[]=(1.0F,1.0F,1.0F,1.0F);
	float vBrillo=this.sliderBrillo.getValue();
	float offsets[]=(vBrillo,Vbrillo,Vbrillo, 0.0F);
	rop = new RescaleOp(Scales,offsets,null);
}else{
	rop=new RescaleOp(1.0F,this.sliderBrillo.getValue(),null);
}
BufferedImage imgdest =rop.filter(imgSource,nul);
